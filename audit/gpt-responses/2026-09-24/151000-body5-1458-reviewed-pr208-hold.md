本轮有新的实质进展：完成了 **5 篇、22 张正文图**的逐图语义核验和冻结物化，但没有在并行生产切换期间抢写，因此本轮新增真正上线仍为 **`published = 0`**。

本轮逐图通过的是：`10.1021/acs.joc.6c01866` 的 Figure 1、Scheme 1–4，共 5 张；`10.1021/acs.orglett.6c03166` 的 Figure 1–3、Scheme 1，共 4 张；`10.1021/acs.orglett.6c03301` 的 Scheme 1–6，共 6 张；`10.1021/acs.orglett.6c03574` 的 Scheme 1–3，共 3 张；`10.1021/acs.orglett.6c03732` 的 Scheme 1–4，共 4 张。22 张全部核对了 task/page/article/source DOI、原始图号和独立图注、capture/job identity、固定世代 `1790082000000`、真实字节、完整 SHA256、content-addressed R2 key、格式/尺寸、文章主题、图片角色、重复/推荐图风险和清晰度。21 个 SVG 均实际渲染并检查了 script、`foreignObject`、事件处理器及外部资源引用；唯一 PNG 正常解码为声明尺寸。所有 Figure 1 都只作为正文图处理，5 篇原有官方 TOC 均为独立对象并保持不变。

本轮没有继续沿用已经变旧的 67-row 快照，而是只启动了一次新的受控只读证据读取：run **`35966705081`** / job `107526802358` 成功，artifact **`10794591083`**，archive SHA256 **`1ecedf73164595b8fff783f8f67802d4d92c3ff5f74558bbd3a2f537579d5729`**。新快照记录 `stageRows=560`、`postCutoverRows=549`、`boundRows=549`、`unpublishedBoundRows=122`、`unpublishedBoundDois=32`、`errors=[]`；在排除已有不可变审批/open reviewed-media PR 和既有 held/scope-review DOI 后，本次 bounded selection 给出 20 个候选 DOI，本轮严格只处理优先级最前的 5 篇。旧快照尾部的 `10.1021/acs.orglett.6c03567` 和 `10.1021/acs.orglett.6c03712` 只是因新鲜候选优先而 deferred，并非 rejected/held。

审批 packet `body5-20260924-1458` 已写入，packet commit 为 **`cea9bb3cd66db88de30477e4b153bd7388c0ca35`**。现有 `Materialize approved body assets` run **`35967197820`** / job `107528358563` 成功，生成 canonical batch `body5-20260924-1458-01`，冻结资产/manifest bot commit 为 **`f00e3b6e7cf6b050fe9423bad6ff9f24a840e79b`**；artifact **`10794209215`**，digest `sha256:ce12df5fefdb32df41e53ad22408e5c2ab9f3d89a4969a4b07a8664ac736c003`。工作流明确给出 `EXACT_REVIEW_PACKET_VERIFIED files=22`、`captionMutationRejected=22`、`productionWrites=0`、`r2Writes=0`、`publisherRequests=0`，22/22 均复用冻结精确字节，`retainedDifferentFile=[]`、`notInCurrentCorpus=[]`、`quarantineUnchanged=true`、static-media integrity `failures=0`。

已建立 **draft PR #208 — `Media: preserve 22 newly reviewed body figures`**。这里有一个重要变化：此前长期阻断的前端质量门禁在本批已经恢复正常。PR head `cd32479b9342569cb42db14affd03eb2a8a8046b` 上，`Body review markers and incremental publication` run **`35967536381`** 成功，`Site quality gate` run **`35967536386`** 也已经全部成功；Frontend build/typecheck、Worker dry-run、Production API smoke、Playwright interaction regression 和最终 Required quality gate 全部为 success。

但我仍没有合并 #208。原因不是本批媒体失败，而是本轮期间 `main` 发生了实际生产站点并行切换：#205 将 Gallery 分享/Pages 主域切换到 `gallery.gczhouwld.com`，随后 main 前进到 **`c1e8a5dbc88186a3b054ee2342280215f9370cf3`**。从本轮起点到该 main 的比较只涉及自定义域、分享/Worker/Pages 工作流以及审计文件，没有生产文献数据变更；但 #208 因此已经与 main 分叉并显示 `mergeable=false`。同时 reviewed-over-auto ownership handoff 的 **PR #192 仍 open/unmerged 且 `mergeable=false`**。按照“遇正式发布/媒体并行部署冲突只保留审批、不抢写”的规则，本轮没有强行 rebase、没有绕过 controller handoff，也没有触发新的生产 Pages 部署。#208 分支还保留了本轮用于启动只读 evidence run 的 `audit/media-review-read-request.json` 变更；它不是生产媒体/文献写入，下一轮应在最新 main 上整理/重放有效审批，而不是重复重新抓取出版社或重复做这 22 张语义审核。

因此当前准确状态是：**本轮 `reviewed_pending_merge = 22 张 / 5 篇`，新增 `published = 0`**。由于没有生产部署，本轮没有执行新增线上 URL/文件哈希/卡片可见性验收，也没有把 materialized、PR 合并前状态或 build-time `added` 冒充上线。fresh bounded selection 中还有 15 个候选 DOI 未在本轮逐图处理，此外还有更后面的积压；这不是正文图全库复核完成的声明。

完整媒体验收已经同步到 `audit/media-acceptance/2026-09-24-150126-body5-1458-reviewed-pr208-hold.md`（acceptance commit **`cd32479b9342569cb42db14affd03eb2a8a8046b`**），本次完整回报已在发送前同步到 `audit/gpt-responses/2026-09-24/151000-body5-1458-reviewed-pr208-hold.md`。