本轮有实际进展，但仍没有把新的正文图标记为 `published`。`body5-20260924-0011-01` 的状态保持为 **18 张 `approved_waiting_deploy`、1 张 held、0 张本轮新增 published**；隔离世代仍是 `1790082000000`，没有修改任何文献卡片、中文标题、收录范围、生产文献数据或 userscript。

我已经把反复出现的 `auto_prior_publication_changed` 定位到更精确的根因：不是 R2/VPN/候选下载/上传/页面发现，也不是那个 nonfatal 的 `/api/media/batch HTTP 500`。问题发生在“人工逐图审核结果接管旧 auto 图身份”时：`merge-approved-body-batches.mjs` 已经用人工审核 publicationId 重建了同一个 DOI+图号，而且完整 SHA256、evidence SHA256 和 label 都没有变化；随后 `merge-new-body-auto.mjs` 仍要求这个历史条目必须继续由 `new-body-auto-20260923-v1` 拥有，于是 fail closed。`10.1021/acs.joc.6c01653` 的 Scheme 3 和 Scheme 6 已确认就是这种**字节与证据完全一致、仅所有权从 auto 转为 reviewed**的情形。

我为这个问题建立了小型媒体控制器 PR **#192**，head 为 `0730b94190008ee029daa1d8aaa43efc960ae1fc`。改动只有 `cloudflare/scripts/merge-new-body-auto.mjs`，相对基线为 9 additions / 5 deletions：只有当当前人工审核 ledger 与媒体条目在 DOI、图号、完整 SHA256、evidence SHA256、label、当前 image URL 和 published 状态全部精确一致时，才允许旧 auto snapshot 条目退出，并记录为 `supersededByReviewed`；任何字节、证据、标签或路径不一致仍继续报 `auto_prior_publication_changed`，不会覆盖人工审核图。现有 **Adaptive paired media batching regression** run `35911224067` 已全部通过，包括代码检查、冻结真实图片 fixture、实际字节验证和历史 publication 回归。

PR #192 **本轮没有合并**，因为仓库级 Site quality gate 当前存在一个与本媒体改动无关、且已经存在于 main 的 TypeScript 错误：`src/card-share.ts(52,10): TS6133 'galleryDeepLink' is declared but its value is never read.` Worker dry-run 和 Production API smoke 均已通过，但前端 typecheck 因这个 main 既有问题失败。按照“不绕过既有质量门禁”的要求，我没有为了上这批图去改这个无关前端文件，也没有强行合并；因此 18 张图继续保持 `approved_waiting_deploy`，没有把“PR 已准备好”冒充上线。

同时，现有只读 salvage inventory 又确认了一个需要隔离的新污染信号。run `35901598663` / job `107318912849` / artifact `10769541329`（归档 SHA256 `9b04da9af6ec76b23709b05badbada0cbc6162d8c25bb4e7cab74315c6314295`）发现 3 个最新自动报告虽然写着 `success`，但 requested/stored DOI 与 report article/source DOI 不一致：`10.1038/s41586-026-11043-z` 实际报告链指向 `10.1021/acscatal.6c06157`；`10.1038/s41467-026-77437-9` 指向 `10.1021/jacs.6c14433`；`10.1002/anie.4947785` 指向 `10.1002/anie.4084841`。这三个 DOI 子集现在按**报告链 DOI/page/source 身份污染**处理并停止审批/上架；没有重抓、没有 direct import/repair、没有全库清理，也没有据此猜测 VPN、R2 或配额故障。该 inventory 因 D1 table read 中存在 HTTP 400，明确是 `byteLevelVerified:false`，所以我没有把它当成字节级恢复授权。

上述结果已经先完整记录到 `audit/media-acceptance/2026-09-24-034800-body-auto-reviewed-handoff-and-contamination.md`，审计 commit 为 **`17ad9fa4d28481c7591ae86310ba411a841af5e9`**。下一步仍保持 fail-closed：等仓库现有质量门禁恢复为绿后，再重读 main/SHA 和并行媒体改动；只有 #192 安全合并、Pages 的 `literature_authorization` 与媒体构建通过，并且部署后逐个核对这 18 张的线上 URL、完整哈希、图号、真实卡片解码和原 TOC 保留后，才会把它们从 `approved_waiting_deploy` 改为 `published`。这仍不是全库正文图复核完成的声明。