本轮继续推进了新的正文图增量，完成 **5 篇、22 张**逐图核验并冻结审批，但没有绕过现有门禁，因此新增 `published=0`。

先刷新了受控只读证据。新的 `Media review queue evidence` run `35952911184` / job `107485071247` 成功，artifact `10789591366`，archive SHA256 `80603e8ecfb3b23e1f230ba9aa539b55490bf117afdc4eec711d46571032ba33`。快照为 `stageRows=535`、`postCutoverRows=524`、`boundRows=524`；扣除已存在于不可变审批批次/当前 reviewed-media PR 的 DOI 以及 held 范围后，仍有 `67` 张、`17` 个 DOI 的未发布绑定记录，`errors=[]`。本轮严格只取优先队列前 5 篇、22 张，没有重新请求出版社、没有 R2/D1 写入，也没有触发 direct import/repair。

本轮逐图通过的是：`10.1021/acs.joc.6c01498` 的 Figure 1–2、Scheme 1–5，共 7 张；`10.1038/s41557-026-02247-x` 的 Figure 1–3，共 3 张；`10.1021/acs.orglett.6c03536` 的 Scheme 1–3，共 3 张；`10.1021/acs.orglett.6c03628` 的 Figure 1–4，共 4 张；`10.1021/acs.orglett.6c03650` 的 Scheme 1–5，共 5 张。JOC `6c01498` 的最终 article-level 报告仍是 `partial / figures=7/8`，所以只批准这 7 张真实存在且逐字节绑定的文件，缺失的第 8 张没有被推定通过；历史 transport checkpoint 也没有被解释成 VPN、R2 或配额原因。

22 张都重新核对了 task/page/article/source DOI、原始图号、独立图注、真实字节、完整 SHA256、content-addressed R2 key、文章主题、图像角色、重复/推荐图风险和当前 public media-index / published-ledger 状态。精确文件构成为 **16 个 SVG、3 个 PNG、3 个 WebP**；16 个 SVG 均实际渲染并通过 active/external-content 检查，3 个 PNG 和 3 个 Nature WebP 均完成解码/签名检查。冻结 stage 中没有发现这些选中源/hash 被其他 DOI 共用。Figure 1 全部严格作为正文图处理，不冒充 TOC。三个 Organic Letters DOI 的现有官方 TOC 为独立来源/哈希并保持不变；本轮 JOC 和 Nature Chemistry 在冻结 public media-index 中没有已部署 TOC，本轮也没有拿正文图替代它们的 TOC。五个 DOI 在冻结 published ledger 中均没有既有正文图，同号 public body figure 也为空，因此本批是纯新增，不覆盖已有正确图片。

审批 packet 为 `body5-20260924-1153`。现有 `Materialize approved body assets` run `35953339685` / job `107486343035` 已成功：`EXACT_REVIEW_PACKET_VERIFIED files=22`、`captionMutationRejected=22`、`productionWrites=0`；canonical batch 为 `body5-20260924-1153-01`，bot asset/manifest commit 为 `56aa6b7f5b08334174f1aaaaba7d6a4dbb7869f2`。materialization artifact `10789352702`，digest `sha256:6a725bc438d7fea3adf9d3c984ba0d6b317c921bf56455af2b7cd1509e0a9216`。原有构建/回归确认 `approvedItems=247`、`retainedDifferentFile=[]`、`notInCurrentCorpus=[]`、`quarantineUnchanged=true`、`stagingWrites=0`、`stagingDeletes=0`，且 protected-literature/static-media 检查通过。

已建立 draft PR **#202 `Media: preserve 22 newly reviewed body figures`**。其媒体门禁 `Body review markers and incremental publication` run `35953489515` 已成功；Site quality gate run `35953489424` 已结束为 failure，但失败仍准确来自 main 既有的 Frontend typecheck：job `107486786508` 报 `src/card-share.ts(52,10): error TS6133: 'galleryDeepLink' is declared but its value is never read.`。同一轮 `Worker dry-run` job `107486786248`、`Production API smoke` job `107486786445`、`Playwright interaction regression` job `107486786353` 全部成功。本媒体任务没有修改这个无关前端问题，也没有降低质量门禁。

PR #192 目前仍 open/unmerged，最新读取在 Git 层面 `mergeable=true`，但 main 仍缺 reviewed-over-auto 的 exact ownership handoff。最终再次重读 main，SHA 仍是 `3adf5f4bf70f42422e31a9c7be9c34da6c217ff4`，没有出现并行生产文献变更。因此 #202 继续保持 draft，不合并、不触发生产 Pages；build-time `added` 不计作上线。当前准确状态是：本轮新增 `reviewed_pending_merge=22` / 5 篇，新增 `published=0`；这份 fresh queue 在本轮 22 张之外仍有 45 张绑定记录等待后续逐图处理。隔离世代继续为 `1790082000000`，文献卡片、中文标题、收录范围、生产文献数据、08:00/18:00 任务和 userscript 均未修改。

完整媒体验收记录已写入 `audit/media-acceptance/2026-09-24-115600-body5-1153-reviewed-hold.md`，门禁/格式计数补充记录写入 `audit/media-acceptance/2026-09-24-120100-body5-1153-pr202-gate.md`。这仍只是本轮受控增量审核，不代表正文图全库复核完成。
