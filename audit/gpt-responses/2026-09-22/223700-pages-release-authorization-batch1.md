核验时间：北京时间 2026 年 9 月 22 日 22:35。上下文：自动更新链路改进，第一批实际发布门禁修复。

**已完成第一批：Pages 真正控制上线的门禁已经恢复，并经过一次实际构建、部署验收。不是只修改定时任务提示词。** 这批代码提交为 `8bd89bf`，验收记录提交为 `e0a6bd8`。fileciteturn550file0L2-L2

### 1. 先明确：一篇待定，不再阻挡其他合格论文

参考文字中的“有一篇 pending 就整批不发”，不采用；按你前面的明确要求，改成**逐篇准入、待定单独保留**。

以你提供的“9 篇纳入、16 篇排除、1 篇待定”为例：候选全集、来源健康、审核证据、快照时效等全局检查通过后，9 篇可以进入固定发布槽；那 1 篇继续保留待定及补证据记录，不上线，也不冒充已经排除。候选漏审、来源故障或版本不一致，仍然阻止整批发布。

新门禁已加入相应检查，并通过“一篇 pending 不阻挡合格子集”“pending 不能混进发布清单”“pending 不能悄悄变成排除”“暂缓记录必须保留证据与下次复核时间”等测试。**这证明了这些规则的程序测试通过，不代表这 9 篇今晚已经发布。**fileciteturn550file0L2-L2

### 2. 实际 Pages 门禁已恢复

原来的 `if: ${{ false }}` 已移除；“这是媒体恢复部署”的文字声明也已移除。现在工作流先运行独立的 `literature_authorization`，构建和部署都明确依赖它成功。门禁会逐一核对 **8 个受保护文献／策略文件的 Git 内容哈希**，不能靠提交说明绕过。fileciteturn550file0L2-L2

文献内容与已授权版本一致时，界面和媒体更新可以继续；文献内容发生变化时，必须有匹配的固定发布槽授权。新发布还要求正式 review、改变的生产数据和发布标记在**同一个原子提交**中落盘，并固定引用 staging、compact handoff 和完整审计的文件 SHA，避免审核的是一个版本、上线的是另一个版本。fileciteturn550file0L2-L2

这些要求也已写入现有正式发布任务及 `docs/publication-release-contract.md`。本批没有更改三个任务的时间，也没有新增同类任务。fileciteturn550file0L2-L2

### 3. 实际验收结果

GitHub Actions **run `35740910761`** 已完成：

| 验收项 | 结果 |
|---|---|
| 本批新增的 14 项回归测试 | 全部通过 |
| 实际文献授权门禁 | 通过，8 个受保护文件哈希全部匹配 |
| Pages 构建、部署 | 均成功 |
| 部署后的搜索过滤 CSS 检查 | 成功 |

本次确实触发了既有 Pages 重建，但**没有修改生产文献数据、审核决定或发布标记**。门禁验证的是原有 512 张卡片对应的 bootstrap 输入基线；512 不是本批重新清点线上卡片得到的数字，也不是新增发布结果。没有启动新一轮文献抓取，没有新增 OA 提图链路。fileciteturn550file0L2-L2

### 4. 后续仍需补齐，不能提前宣布全部完成

这批先解决了最危险的“检查存在，但真正部署时跳过检查”。**确定性的 staging→正式 review 转换、旧后验校验器兼容性、构建产物与线上同版本 DOI 验收，尚未在本批完成验收。** 原有 20 分钟技术上限与固定时点规则的统一、审计去重、状态归并和通知配置，也没有算作已完成；通知／邮件开关本批未更改。fileciteturn550file0L2-L2

下一批优先接上“正式审核转换 → 仓库内一致性检查 → 部署后同版本验收”，避免把正常审核误报失败，或因为验收先于部署而读到旧网站。当前可以确认的是：**实际 Pages 门禁已恢复并跑通；完整的新文献原子发布链路，仍需继续验收。**
