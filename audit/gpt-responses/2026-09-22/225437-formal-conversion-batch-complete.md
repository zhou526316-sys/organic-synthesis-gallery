核验截至北京时间 **2026 年 9 月 22 日 22:54**。上下文：自动更新链路第二批——正式审核转换。

**第二批已经完成并经过实际 GitHub Actions 验收：新增固定转换程序，避免发布时临时拼接或直接重命名审核 JSON。** 功能提交为 `d5a2036`，本批验收证据提交为 `71f0aa4`；这不是一次新文献发布。fileciteturn559file0L2-L2

### 1. 审核结果现在由固定程序转换

新增 `scripts/convert-prepublish-review.mjs`，把 staging 的 `decisions[]` 转成正式审核所需的 `accepted[] / rejected[] / pending[]`，保留原有 DOI、日期、作者、审核依据和两遍结论。正式结构缺少 `reason` 时，只复制已有的 `evidenceBasis` 原文，不编造新理由；缺失关键证据、计数不符或最终结论与 challenge 不一致，会明确失败。程序同时生成待定队列及下一固定复核槽，并绑定输入文件与输出内容的 Git SHA，减少“审核一个版本、发布另一个版本”的风险。fileciteturn559file0L2-L2

**一篇 pending 不会因此重新拖住其他文献。** 待定项保留在 `pending[]` 和补审队列，不进入可发布 DOI 清单，也不会在转换中变成排除项。fileciteturn559file0L2-L2

### 2. 已用测试样例和当前真实审核记录验证

实际运行是 **`35742939330`**，结果如下：

| 检查项 | 实际结果 |
|---|---|
| 新增转换回归测试 | 30 项全部通过 |
| 原有审核、时效、逐篇发布及待定一致性测试 | 34 项全部通过 |
| 当前 26 条审核记录的转换预览 | 通过，仍为 9 纳入／16 排除／1 待定 |
| 已审核 DOI 子集的语义发布检查 | 通过 |
| 明早发布槽的快照新鲜度检查 | 未通过；严格发布包生成步骤被跳过 |

也就是 **64 项回归测试全部通过，但本次工作流总体仍是 failure**。唯一失败条件是今晚的快照不属于明早的有效预发布窗口，不能拿今晚的测试输入冒充明早新审计；不是那一篇 CCS 待定造成阻断。这里的 26 条是对已有审核记录做转换验收，不是本批重新审核了 26 篇论文。fileciteturn559file0L2-L2

### 3. 发布任务已接上转换要求，未提前发布

现有正式发布任务已同步要求使用通过严格检查、且 SHA 与本槽输入一致的转换结果；预览文件或被跳过的严格步骤不能当作发布许可。**08:00／18:00 的安排保持不变，没有增加任务。** 本批没有修改生产文献、审核决定、发布标记或协调状态，没有触发 Pages 部署，也没有重新抓取文献或运行 TOC／OA 提图。fileciteturn559file0L2-L2

目前完成的是“审核记录 → 正式结构 → 发布证据绑定”这一段。**生产数据与标记的自动原子写入、构建产物 DOI 对账、部署后同版本在线验收，仍未在本批完成，不能把 64 项测试通过等同于整条自动发布已经验收。** 下一批优先补这段实际发布与验收的衔接，不再增加同类定时任务。fileciteturn559file0L2-L2
