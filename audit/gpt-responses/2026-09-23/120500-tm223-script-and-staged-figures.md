# Bridge 2.2.23 delivery and staged body-figure clarification

Beijing context: 2026-09-23 12:05 +08:00; actual primary live check 11:56:37.714 +08:00.
User: failures should be analyzed and fixed with updated script/code delivered; asks whether new body figures are all staged because cards only show TOC.
Functional merge: 8b34a9b96fc7dbb6964460abc6bd5faef5875a73 (#148).
Primary live evidence: run 35816320730, artifact 10731238433; independent ZIP/script hash checks completed.
Detailed audit: cfe7a5e4ce1179409af46465571cdf9f11bb872c.

## Progress replies

这批先集中排查正文图暂存时的 503：读取最新自动报告，核对服务端具体在哪一步失败，再做针对性修复。已恢复的 TOC、主图和隔离规则保持不变，不让你重复整篇重抓。

对，接下来要交付的是修复后可直接更新的脚本，而不是只给分析结论。

你看到卡片只有 TOC，也与目前的发布机制有关：正文图保存成功后仍停在暂存区，尚未自动进入卡片图集；上传报 503 的图片则不能算保存成功。我现在核对暂存库存、网页实际可见数量和失败响应，把“抓不到”“保存失败”“已保存但未展示”分开处理。

已核对：北京时间 11:36，隔离时间之后的正文图暂存索引有 198 条记录。这说明不能把“卡片只显示 TOC”理解为“正文图没抓到”；但这 198 条还不等于已经逐图验收并发布。

修复版 2.2.23 已通过本轮上传重试、存储保护和浏览器回归测试，正在跑合并前检查。它会保留已下载图片，只重试上传，并记录具体响应类型、请求编号和存储失败步骤；不会把失败算成已保存。此次尚未打开正文图自动发布。

## Final user-visible reply

**更新脚本已经交付：Bridge 2.2.23 已正式上线，安装文件和 Worker 新版存储接口均已核验。** 这版修复的是上传失败处理和存储保护，**尚未开启正文图自动发布**。fileciteturn719file0L2-L2

**[点击安装／更新 2.2.23](https://zhou526316-sys.github.io/organic-synthesis-gallery/gallery-vpn-bridge.user.js?v=2.2.23)**　｜　[完整脚本代码文件](sandbox:/mnt/data/gallery-vpn-bridge-2.2.23.user.js)

### 正文图现在到底在哪里？

**保存成功的新正文图仍在服务器 R2 暂存区；但不能说所有抓取尝试都已经暂存。** 上传失败、超时或没有有效保存回执的图片，不计作保存成功。北京时间 **9 月 23 日 11:56** 的实际线上核验结果是：fileciteturn719file0L2-L2

| 环节 | 实际记录 |
|---|---:|
| 正文图暂存接口 | **207 条图片记录** |
| 网页发布索引中的图集图片 | **5 条图片记录** |

这两项是不同库存，不能直接相减当作精确的待发布数量。**你看到大多数卡片只有 TOC，主要是“已保存 → 核验 → 正式展示”这一步尚未完成，并非正文图完全没抓到。** 207 条也不代表我已逐图检查过其内容、图号和清晰度。fileciteturn719file0L2-L2

### 2.2.23 具体修复了什么

**上传偶发失败，不再立即放弃已下载图片。** TOC 和正文图都加入了最多 **3 次上传尝试**：同一任务内保留已经取得的图片，只重试提交，不重新向出版社下载；遵守服务器要求的等待时间，遇到 DOI 错绑、权限拒绝或任务已失效则停止。这里是任务内重试，不是关闭标签页后仍永久保存所有未上传图片。fileciteturn719file0L2-L2

**存储失败不会先删掉旧图。** 修正了旧流程“新图索引还没写成功，就可能删除旧图片”的风险；索引损坏时停止写入，索引被其他任务更新时重新读取再合并，避免覆盖另一篇刚保存的记录。fileciteturn719file0L2-L2

**失败报告更具体。** 现在会保留响应类型、请求编号、Worker 实际错误码、失败在图片写入还是索引写入，以及图片文件是否已经保存。没有返回具体错误码的 503 仍明确记为原因未确定，不能把它直接说成“配额耗尽”。新增 **25 项上传／存储测试通过**，原有安全与浏览器回归也通过；你本机运行 2.2.23 后的真实结果仍需核验。fileciteturn719file0L2-L2

更新时先暂停抓取、等任务页关闭，再点击上面的链接原位更新；**不要卸载、清空密钥或断点记录**。随后刷新唯一的 Gallery 控制页，继续抓取即可。日志里的协议版本仍可能是 **6.2.20**，本次安装版本应为 **2.2.23**。fileciteturn719file0L2-L2

**下一批优先处理已经暂存的正文图核验和上架，不再让你靠重复抓取解决“网页看不到”的问题。** 本次更新不会让这 207 条记录立即全部出现在卡片上，也没有删除它们或改动已恢复的 TOC。
