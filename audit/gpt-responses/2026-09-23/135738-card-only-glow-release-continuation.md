# UI / feedback continuation

Beijing record reference time: 2026-09-23 13:57:38 +08:00.
User request: 继续. Controlling correction: 光效粗细说的是卡片的光效，按钮不需要.
Repo: zhou526316-sys/organic-synthesis-gallery; main baseline.
Merged PR147 exact tested head299777da6c10617f6b370f08fb8d13215938fabf as516df79c6b86593ab841314b2f0628e6780578aa.
Evidence: audit/ui-feedback/2026-09-23-135503-card-only-glow-release.json, commitbfcd785b1c1b0ba0af7e86608eadf5f4c20f5cc7.
Final live run35823626613:Worker14/14;GitHub12/14. All source checks passed before merge. No feedback status write this turn;32 stays open. Fresh queue35823175854 includes33/34.

## User-visible progress messages in this turn

继续按“整张卡片发光、按钮不发光”的要求收尾。我先核对 PR #147 和最后一组 GIF 诊断结果，再处理阻塞上线的问题。

卡片光效和粗细测试已通过；阻塞仍是 GIF 检查。最后一组对照排除了弹窗定位方式，我会继续定位动画停帧原因，不再反复改卡片光效。

已有对照显示，同一 GIF 放到页面左上角能播放，但原位置的局部截图不变。这提示截图方式可能影响结果；我会用整屏截图中的同一区域交叉验证。

新的对照把问题缩小到了状态弹窗的样式：去掉该组件样式后，同一 GIF 能播放；更换文件地址、弹窗定位和截图方式都没有解决。我会针对样式继续定位，保留原有图片数据。

检查样式时出现了一个重要反例：仅读取样式规则、没有删除任何规则，GIF 也通过了。因此暂时不能归因于某条 CSS；我会核对这种检查行为是否干扰了测试，避免给网页加无效补丁。

截屏工具本身会临时插入样式，可能触发页面重新处理内容。我已增加一组只改变截屏准备步骤的对照，原图、动画和刷新恢复检查保持不变。

对照结果明确了：取消截屏时的临时样式插入后，手机、桌面的 GIF 播放和刷新恢复均通过。我接下来收窄展示层的变动监听，再用原来的测试工具复测，不改图片获取逻辑。

修复候选已提交：无关的页面头部变动不再触发整批卡片扫描，新增卡片和语言切换仍走原来的更新流程。现在正用未修改的浏览器测试库执行完整回归。

收窄监听后，卡片和性能检查通过了，但旧版 WebKit 的两项 GIF 检查仍未通过，所以没有合并。我会用当前正式浏览器版本交叉验证同一套断言，确认是否存在版本差异。

新版本交叉测试也复现了同样问题。已确认是截屏准备步骤会干扰 GIF 采样；我会把修正限定在测试工具中，保留六秒内必须捕获不同真实帧的检查，不再给网页添加试探性补丁。

新吐槽已读到：#33 希望增加回到顶部/底部按钮，#34 希望手机图片支持双指缩放。这两条已保留，本批先完成卡片光效的上线验收。

此前反复失败的原图/GIF 专项现已通过，原图字节、刷新恢复和静态图片负对照都保留检查。接下来核对其余检查，并验证正式网页上的卡片边框与按钮无光效。

PR #147 已通过全部七项检查并合并。现在部署的是“整张卡片可调光效、按钮不发光”的版本；我正在检查两个公开入口是否实际加载了它。

两个公开入口都已加载新版本，且已确认光效控件作用于卡片、按钮没有装饰光效。完整线上回归正在检查拖动、保存、刷新恢复以及 GIF 原图是否仍正常。

整张卡片发光、按钮不发光已在两个入口实测确认，GIF 检查也都通过了。Worker 为 14/14；GitHub 为 12/14，剩下两项多次操作、刷新组合测试超过了 30 秒时限，我会保留这两项性能验收遗留。

## Final user-visible reply

**卡片光效纠正已上线到 GitHub Pages 和 Worker 两个入口。现在可调粗细的是整张文献卡片，按钮和状态小标签不再添加这套光效。** 对应 PR #147，发布提交为 `516df79`。fileciteturn721file0L4-L16

刷新网页后，点击卡片的“阅读状态”，在对应状态下调整 **“卡片光效”“卡片光效粗细”**。支持关闭、柔光、呼吸、环绕和彩虹，粗细为 **1–6px**；拖动时预览，松开保存，也可以恢复默认。已有设置沿用到卡片，不需要重新配置；按钮原来的颜色、图片和状态文字保留。fileciteturn720file0L2-L2

[查看手机端整张卡片光效](sandbox:/mnt/data/card-glow-live-mobile.png) · [查看桌面端效果](sandbox:/mnt/data/card-glow-live-desktop.png)

截图使用隔离测试设置，后端接口被模拟，因此其中的图片占位和阅读人数不代表真实抓图状态或访问统计。fileciteturn720file0L2-L2

### 验收结果

合并前七项检查全部通过：卡片专项在 WebKit、Chromium 中各 **11/11**，原图专项 **18/18**。上线后，**Worker 为 14/14，GitHub Pages 为 12/14**。两个入口的卡片作用范围、按钮无光效，以及手机和桌面的 GIF 完整流程都已通过。fileciteturn720file0L2-L2

GitHub 保留的两项失败是桌面拖动组合、异常设置连续刷新组合超过 **30 秒整体测试时限**；没有扩大时限或把失败记录删除，**第 32 条暂未整体关闭**。此前 GIF 验收的采样干扰也已定位并修正了测试截屏驱动，真实帧变化、原图字节校验和静态图反向检查仍保留。fileciteturn720file0L2-L2

本批没有修改文献数据、抓图获取逻辑或真实阅读计数。新增 **#33“回到顶部/底部”**、**#34“手机双指缩放”**已接收但尚未实现；智能裁切、阅读人数偏少等问题仍保留，备用站未在本轮复测。fileciteturn720file0L2-L2

[查看本批验收记录、对照结果与截图](sandbox:/mnt/data/card-glow-release-evidence.zip)
