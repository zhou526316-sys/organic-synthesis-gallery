# Body publication + Bridge 2.2.24 — final user reply

Beijing context: 2026-09-23 ~18:56. This reply is synchronized before user-facing delivery.

**本轮两条线都已经完成真实上线验收。**

正文图方面，刚才这一小批 **5 篇、18 张** 已经正式上线，不是 staged/merged 状态。对应 DOI 和新增正文图数：

| DOI | 新增正文图 |
| --- | ---: |
| 10.1021/acs.orglett.6c03473 | 4 |
| 10.1021/acs.orglett.6c03511 | 1 |
| 10.1021/jacs.6c10810 | 3 |
| 10.1021/acs.orglett.6c03340 | 6 |
| 10.1021/acs.orglett.6c03636 | 4 |
| **合计** | **18** |

PR #164 合并后，正式 Pages 部署 run 35849359647 成功，`literature_authorization`、build、deploy 全部通过。随后我没有沿用上一轮 20 篇/75 张的验收结果，而是把可复用 verifier 明确切到本轮 packet `body5-20260923-1815-a`。最终线上验收 run 35849859685 / job 107144704261 成功：**18/18 实际公开文件哈希正确、5/5 实际卡片可见、全部新图 URL+图号标签匹配并正常解码，browserErrors=0**。五篇原有官方 TOC 的 contentHash 也全部保持不变。验收 artifact 10745331743 的 ZIP SHA256 为 `53042eff7ff0b88a8d6fd803587feb5c2da5868f4a5afe1a065cd6c814e9f3d4`。

脚本方面，**Bridge 2.2.24 已经正式上线并通过两套独立线上检查。**

**[点击安装／更新 Gallery VPN Bridge 2.2.24](https://zhou526316-sys.github.io/organic-synthesis-gallery/gallery-vpn-bridge.user.js?v=2.2.24)**

固定更新地址仍是：
`https://zhou526316-sys.github.io/organic-synthesis-gallery/gallery-vpn-bridge.user.js`

2.2.24 不是纯版本号更新。它针对 2.2.23 的真实报告修了两个具体问题：

1. **ACS 高清正文图 TIFF/octet-stream 问题。** 一些高分辨率 Figure/Scheme 下载端点返回 TIFF 字节，但 HTTP 类型只是 `application/octet-stream`。旧版可能连续试完前四个高清候选后，就没有再尝试同一张 Figure/Scheme 页面里实际已经加载的图片。2.2.24 会明确识别 TIFF，但不会把 TIFF 原样塞进当前 Worker；普通候选失败后，只允许使用**同一个已隔离图号 DOM 区块**中的实际 `currentSrc` 作为最后回退，而且 ACS 回退 URL 必须编码当前任务 DOI。原来的清晰度门槛没有降低。

2. **抓取顺序。** 现在先按上线日期从新到旧；同一天先抓缺官方 TOC 的文章；再优先 JACS；最后保持原队列顺序。因此“已有 TOC 的 JOC 抢在同日缺 TOC 的 JACS 前面”的情况已被修正。已有 TOC 的文章仍只补正文图，不会覆盖 TOC。

关键防污染规则全部保留：`capture_tab_job_mismatch`、`page_doi_mismatch`、`media_source_doi_mismatch` 的线上检查均为 true。内部 `captureVersion` 仍是 **6.2.20**，这是有意保持 Worker、断点和已保存记录兼容；判断这次客户端更新应看安装包版本 **2.2.24**。

发布前真实 Chromium 回归新增 11 项并全部通过，包括“四个 TIFF/octet-stream 高清候选均失败后同图 currentSrc 成功回退”和“foreign DOI currentSrc 必须被拒绝”。正式 Pages 部署 run 35850281190 成功。最终只读线上验收：
- run 35851366429：2.2.24 版本、控制器、6.2.20 协议、同图回退、TIFF 识别、新排序、三项 DOI guard、无 eval，全部 true；
- run 35851366440：实际公开 userscript `@version 2.2.24`，文件 SHA256 `e3435023fe633dd3c758acfc7988cdb3e52a90ed2031c620110938f7a1d87604`，UTF-8 178201 bytes。

你现在更新时**不要卸载旧脚本、不要清空 Tampermonkey 存储、不要重新填密钥，也不要删除断点**。如果当前还有文章任务正在跑，先暂停并等当前任务页结束/关闭，再点上面的链接原位更新；随后刷新唯一的 Gallery 控制页并继续抓取即可。

已有正文图不需要因为升级而重新抓。符合当前自动增量发布规则的 ACS 新正文图会继续走增量上架；异常、来源不透明、图号/证据变化或同图替换仍会单独拦截审核。2.2.24 是否在你本机明显提高正文图成功率，下一批自动报告会给出真实结果；本轮验收验证的是公开安装包和功能契约，不把它冒充成你本机未来抓取效果的实测。

Detailed audit records:
- audit/media-acceptance/2026-09-23-183846-body5-live.md
- audit/media-acceptance/2026-09-23-185335-tm224-live.md
