# JACS body-media maintenance batch — live acceptance

Recorded: 2026-09-23 16:28:56 Asia/Shanghai
Task scope: media-only review/publication of user Tampermonkey/VPN Bridge body-figure captures already stored in R2. This record does not authorize or modify literature-card publication, titles, scope, the fixed 08:00/18:00 literature release tasks, capture settings, userscript release, or whole-library promotion.

## Source evidence and bounded review

The reviewed batch reused the controlled read from Actions run `35833001369`, artifact `10737978319`, archive SHA256 `d2ca8159ecdabb4ee21aa389f92f621de0c962eec657ba9514e31f584ded782c`. The read was storage-only/read-only and did not fetch publisher pages again. The approved maintenance subset was bounded to five current-corpus JACS articles and fourteen exact figure files, within the <=5 article / <=30 image contract.

| DOI | Newly approved body media |
| --- | --- |
| `10.1021/jacs.6c09678` | Scheme 3 (1) |
| `10.1021/jacs.6c13635` | Figure 3, Figure 4 (2) |
| `10.1021/jacs.6c13760` | Figure 1, Figure 2 (2) |
| `10.1021/jacs.6c13989` | Figure 1, Figure 2, Figure 3, Figure 5 (4) |
| `10.1021/jacs.6c15781` | Figure 1–Figure 5 (5) |

Each approval manifest binds DOI, normalized figure ID/label, independent caption, article/source URLs, capture version/page/task identity, original capture time/R2 key, full SHA256, dimensions/content type, and canonical `evidenceSha256`. Approval is explicit per file; the server capture marker alone is not treated as semantic approval.

The five manifests are `body-maint-6c09678-20260923-1548`, `body-maint-6c13635-20260923-1548`, `body-maint-6c13760-20260923-1548`, `body-maint-6c13989-20260923-1548`, and `body-maint-6c15781-20260923-1548` under `audit/media-recovery/body-batches/`.

## Materialization, review gates and publication

Controlled materialization run `35834507427` succeeded. It re-read exact stored objects, verified hashes and approval fingerprints, exercised the existing static merge/integrity path, and confirmed no protected literature input changes. It did not mutate or delete the original R2 staging objects.

PR #158 head `5dd40e90311a1d57f1d32099ef9d23f37380d1a6` contained only the 14 content-addressed media assets, five approval manifests and the existing Pages refresh marker. Body-media regression run `35834666953` and Site Quality Gate `35834666815` both succeeded. PR #158 was squash-merged as `262934dbe8b3534c0f8fff77ea1f1e8fe8d20e16`.

GitHub Pages run `35835289087` completed successfully, including `literature_authorization`, build and deploy. A later unrelated UI deployment, run `35836549349` on main, also completed successfully with its own literature-authorization gate and rebuilt the same approved media manifests. Its Pages artifact `10739581457` has archive SHA256 `c373cd3a62a4d1c9416cb88465e3b083b1651461be8572a43405d34950f503d3`; inspection of that newer artifact confirms all 14 new DOI+figure+SHA approvals remain present in `media-index.json`, `body-publication-ledger.json` and their content-addressed static files. The five target DOI retain `toc.available=true`. The official-TOC live workflow `35835582938` also completed successfully after the media deployment.

The current approved-body build status in the newer Pages artifact reports: `approvedItems=45`, `published exact approved body-batch items=45`, `retainedDifferentFile=0`, `notInCurrentCorpus=0`, `ledgerCount=75`, `totalPublishedFigureEntries=81`, `quarantineUnchanged=true`, `stagingWrites=0`, `stagingDeletes=0`. The 81 figure entries are an online inventory count, not a claim that every paper's complete figure set has been recovered. The publication ledger count includes exact reviewed files from earlier accepted body-media recovery generations as well as this batch.

## Direct current-live verification

The previous live verifier was hard-coded to the legacy first body batch, so it was not used as proof for this maintenance batch. A small verifier-only PR #159 changed only `scripts/verify-reviewed-body-live.mjs`; Site Quality Gate run `35836606166` passed all jobs. It was merged as `1b7aa359752f3a5e44d5f44fec0b5525a32ae8b1`. This is a durable generalization of the existing live verifier, not a new per-batch publication path.

Read-only trigger commit `c190d5d20ce7e0a0e258ae00310d4690da826c2b` started `Verify reviewed body figures live` run `35837208982`. The run completed SUCCESS. Artifact `10739039498` was downloaded and its archive SHA256 independently recomputed as `dbe229e517e306ee9a48995b322db2822c84a81bd04d6d2a1abf69020f18b3bc`, matching GitHub's artifact digest.

Acceptance timestamps: checked `2026-09-23T08:28:42.429Z`; completed `2026-09-23T08:28:56.323Z` (16:28:56 Beijing). Result `passed`; `productionWrites=0`; `browserRenderingVerified=true`; browser error list empty.

The generalized acceptance fetched and SHA256-verified all 45 currently approved body-batch items against the live publication ledger, live `media-index.json`, and live static image bytes. All 45 exact DOI+figure+SHA entries matched; `retainedDifferentFile=0` and `notInCurrentCorpus=0`.

It then opened the real production Gallery in Chromium with every non-GET/HEAD/OPTIONS request blocked and checked the five most recently reviewed article groups. All five figure strips were visible, all images decoded with positive natural dimensions, and every exact approved image URL expected for each article was present:

| DOI | Current card figure entries checked | Exact approved images checked | TOC retained |
| --- | ---: | ---: | --- |
| `10.1021/jacs.6c09678` | 6 | 6 | yes |
| `10.1021/jacs.6c13635` | 2 | 2 | yes |
| `10.1021/jacs.6c13760` | 2 | 2 | yes |
| `10.1021/jacs.6c13989` | 4 | 4 | yes |
| `10.1021/jacs.6c15781` | 5 | 5 | yes |

The six figures currently shown for `6c09678` include previously approved body figures plus this batch's new Scheme 3; the maintenance batch itself added fourteen files total, not nineteen.

## Holds, next queue and safeguards

The four `10.1021/jacs.6c13641` candidate files remain outside this batch under the existing scope-review hold; this is not a contamination finding and they were not deleted or re-downloaded. Three `10.1021/jacs.6c10810` candidates seen in the earlier bounded evidence read were not included in this five-paper batch and remain pending a later review; no failure cause is asserted here.

A newer controlled read, run `35836464378`, completed without read errors. Artifact `10739276946` has SHA256 `4adb4a39d753eedb27927adfd5c4cd21f2791d912b67f76c68aaa7d2b9d8bc0c`. It is retained for the next bounded review rather than starting a second >5-paper batch in this run. Its staging selection includes `10.1021/jacs.6c13517`, which is not eligible for media approval because that DOI is no longer in the current corpus; future review must filter it out rather than treating a successful capture as publication authorization.

No new cross-DOI contamination or major repeated capture/storage failure was established in this run. The media generation/quarantine epoch remains `1790082000000`; original R2 stage objects and real timestamps remain intact; whole-library automatic promotion remains disabled. This acceptance is limited to the explicitly reviewed/published files above and is not a claim of full-library media completeness.
