# Body media acceptance — 2026-09-24 13:23 CST

## Scope and baseline

Media-only continuation for `zhou526316-sys/organic-synthesis-gallery`; `main` is the sole production baseline. The round began from and, immediately before the publication decision, re-read unchanged main SHA `7909b43eecc30327341a8f3305b6facd906e45f6`. No literature card, Chinese title, inclusion scope, production literature payload, 08:00/18:00 literature task, userscript, direct publisher-import path, or quarantine generation was changed. Fixed media generation remains `1790082000000`.

The current main still contains the existing `galleryDeepLink` helper in `src/card-share.ts`; this media task did not modify unrelated frontend code.

## Reused controlled evidence

This round reused the still-valid frozen, read-only queue evidence rather than restarting the same evidence collection:

- evidence run: `35952911184`
- evidence artifact: `10789591366`
- archive SHA256: `80603e8ecfb3b23e1f230ba9aa539b55490bf117afdc4eec711d46571032ba33`
- frozen snapshot: `stageRows=535`, `postCutoverRows=524`, `boundRows=524`, evidence errors `[]`
- prior review/hold exclusion queue at that snapshot: 67 rows / 17 DOI
- previous batch `body5-20260924-1153` consumed 22 rows / five DOI; this batch consumes the next 20 rows / five DOI, leaving about 25 frozen queue rows for later bounded review

No publisher request, R2/D1 write, server-side direct import/repair, login bypass, or userscript publication was performed. Historical capture/controller/transport failures are retained as provenance only; no VPN, quota, R2, or other root cause is inferred from a status code alone.

## Individually reviewed increment

Review packet: `body5-20260924-1318`.

Five current-corpus papers / twenty exact stored body images passed individual semantic, provenance and byte-level review:

- `10.1021/acs.orglett.6c03773`: Scheme 1–4 (4).
- `10.1021/jacs.6c07049`: Figure 2 and Figure 6 (2). These exact files were preserved from an earlier partial `2/7` capture; a newer report is `0/7`. Only the two stored files are approved; missing figures are not inferred approved.
- `10.1021/jacs.6c08636`: Scheme 2 (1). The article-level capture remains partial `1/7`; only the exact stored Scheme 2 file is approved.
- `10.1021/acs.orglett.6c03180`: Figure 1 and Scheme 1–6 (7).
- `10.1021/acs.orglett.6c03293`: Scheme 1–6 (6).

For every item the frozen task/stage binding, DOI-bearing page/article/source evidence, original Figure/Scheme identity, independent caption, exact byte length/type/dimensions, full SHA256, content-addressed R2 key, capture/job identity, article-topic consistency, body-image role, duplicate/recommendation risk, and frozen public media-index state were checked. All selected full SHA256 values and byte lengths matched the stored evidence exactly; selected hashes are unique.

The exact format mix is 17 self-contained SVGs and 3 PNGs. All SVGs rendered and contained no `script`, `foreignObject`, inline event handler, `javascript:` URL, or external href. All PNGs decoded successfully. Visual review found all selected structures, scopes, optimization/mechanistic panels and analytical/modeling panels consistent with their captions and owning papers, with no unrelated/recommendation contamination observed.

Frozen public `media-index` records have no same-ID body figure for these five DOI. Existing official TOCs are distinct objects/hashes and are preserved. The `10.1021/acs.orglett.6c03180` Figure 1 is explicitly a body Figure 1 and is not a TOC substitution.

## Controlled materialization

An exact review packet was committed on isolated branch `media/body-batch-review-20260924-1318` at `b88ac3e4c3b91373a5173abc5cf854a0fa550faf`. The existing `Materialize approved body assets` workflow then ran once and completed successfully:

- run: `35959336758`
- job: `107504330549`
- result: success
- exact packet validation: `files=20`, `manifests=1`, `captionMutationRejected=20`, `productionWrites=0`
- canonical batch: `body5-20260924-1318-01`
- bot asset/manifest commit: `71ee68146f41abe2977ea0afa4ef1526be5477da`
- materialization artifact: `10791458362`
- artifact digest: `sha256:2418fd569852503f4ea9e92eeb6d3c6f652dff7eb4eb1e52f33fc3515ffa01b9`
- explicit side effects: `r2Writes=0`, `publisherRequests=0`
- builder: `approvedItems=245`, `alreadyPublished=[]`, `retainedDifferentFile=[]`, `notInCurrentCorpus=[]`, `quarantineUnchanged=true`, `stagingWrites=0`, `stagingDeletes=0`
- unchanged publication-builder/static-media regressions and protected-literature checks: passed

A compare from exact main `7909b43...` to the materialized head showed only the 20 immutable body assets, the canonical batch manifest, and the review packet. No protected literature file was changed.

## PR/gate status and publication hold

Draft PR `#203` — `Media: preserve 20 newly reviewed body figures` — was opened from the isolated branch against main. `Body review markers and incremental publication` run `35959468864`, job `107504738560`, completed successfully; marker non-authorization and exact-ledger rebuild checks passed.

Site quality gate run `35959468952` is not green. `Production API smoke` job `107504738597` succeeded and `Worker dry-run` job `107504738774` succeeded. `Frontend build and typecheck` job `107504738651` failed specifically at the `Type-check frontend` step, after which its build/artifact steps were skipped. The unrelated frontend source on current main still contains the pre-existing unused `galleryDeepLink` helper; this media-only task does not repair or bypass that gate. `Playwright interaction regression` was still running when this acceptance record was finalized, but the already-failed required frontend gate is sufficient to prohibit merge.

PR `#192` for exact reviewed-over-auto ownership handoff also remains open/unmerged, so current main still lacks that controller handoff. Accordingly PR #203 stays draft: no merge, no production Pages deployment, and no live publication claim.

Current status for this increment is `reviewed_pending_merge=20`, `published=0`. Materialization/build-time `added` entries are not deployment evidence. There is therefore no new live URL/hash/card/decode verification to report yet. This record covers only this bounded increment and does not assert full-library body-media review completion.