# Build contract

The Pages build must merge `public/curated-supplement.json` after mirroring historical metadata and before the Vite build. It verifies a known curated DOI and its Simplified Chinese title in the final static artifact.
